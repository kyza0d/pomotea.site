export const metadata = {
  title: "Resources - Pomotea",
  description: "Explore our collection of productivity resources, tutorials, and guides to maximize your workflow efficiency.",
};

export default function ResourcesLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}